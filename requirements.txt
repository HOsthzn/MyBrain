pyyaml>=6.0
flask>=3.0

# ML / Semantic search (optional but recommended)
# Enables semantic vault scanning, duplicate detection, and similar-note discovery.
# Works on Python 3.14+ — no ChromaDB or pydantic dependency.
# The server runs fine without these — embeddings features degrade gracefully.
sentence-transformers>=2.7.0
hnswlib>=0.8.0
numpy>=1.24.0